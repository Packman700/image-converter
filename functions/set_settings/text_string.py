def text_string(CHAR_MODE):

    if CHAR_MODE == "random_characters" or CHAR_MODE == "next_characters":
        return input("Write string with you want to use in picture: ")
    else:
        return None
